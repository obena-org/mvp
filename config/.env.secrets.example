# =============================================================================
# KPA PoC — required keys (rnd-kpa only)
# =============================================================================
# Preferred: inject via Doppler (doppler run -- python -m rnd_kpa "query")
# Fallback:  copy this file to config/.env.secrets and fill in values

ANTHROPIC_API_KEY=
FIRECRAWL_API_KEY=
